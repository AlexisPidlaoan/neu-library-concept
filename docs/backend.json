
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account within the StudyFlow application, including their profile and access status.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's institutional email address, used for authentication. Must be from @neu.edu.ph domain.",
          "format": "email"
        },
        "studentId": {
          "type": "string",
          "description": "The user's unique student/employee ID (format: 12-34567-890)."
        },
        "displayName": {
          "type": "string",
          "description": "The user's full name as provided by their authentication provider."
        },
        "isBlocked": {
          "type": "boolean",
          "description": "A boolean indicating if the user is currently blocked from accessing the application."
        },
        "role": {
          "type": "string",
          "description": "The user's role within the application (e.g., 'student', 'admin')."
        },
        "photoURL": {
          "type": "string",
          "description": "URL to the user's profile picture."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the user account was first created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "The timestamp when the user account information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "isBlocked",
        "role",
        "createdAt"
      ]
    },
    "College": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "College",
      "type": "object",
      "description": "Represents an academic college within the institution, which users can select during check-in.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the College entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the college."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the college entry was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt"
      ]
    },
    "VisitLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "VisitLog",
      "type": "object",
      "description": "Records an individual user's visit to the library, including purpose and associated college.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the VisitLog entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who logged this visit."
        },
        "userName": {
          "type": "string",
          "description": "Cached name of the user at the time of visit."
        },
        "userEmail": {
          "type": "string",
          "description": "Cached email of the user at the time of visit."
        },
        "college": {
          "type": "string",
          "description": "The college or office selected during check-in."
        },
        "purpose": {
          "type": "string",
          "description": "The reason for the library visit."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp when the user checked into the library.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "userName",
        "college",
        "purpose",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information. Users can read and update their own profiles. Administrators can list and block users.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user."
            }
          ]
        }
      },
      {
        "path": "/colleges/{collegeId}",
        "definition": {
          "entityName": "College",
          "schema": {
            "$ref": "#/backend/entities/College"
          },
          "description": "Lookup table for colleges/offices.",
          "params": [
            {
              "name": "collegeId",
              "description": "The unique identifier for a college."
            }
          ]
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "VisitLog",
          "schema": {
            "$ref": "#/backend/entities/VisitLog"
          },
          "description": "Records individual library visit entries.",
          "params": [
            {
              "name": "visitId",
              "description": "The unique identifier for a visit log entry."
            }
          ]
        }
      }
    ]
  }
}
